Create a github markdown document explaining how exactly 
to create a VS Code plugin for the python dotprompt-cli package that would allow running .prompt files.

When a .prompt file is run form inside VS Code, it should be executed using the `dotprompt` cli command.
This cli command can be installed using `pip3 install dotprompt-cli`
